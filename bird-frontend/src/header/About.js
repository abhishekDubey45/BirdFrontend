import react from 'react';
import ReactDOM from "react-dom/client";
import { BrowserRouter as Router, Routes, Route,Link } from "react-router";

export const About = () => {

    return (
        <div>
            <h1>About Us</h1>
            <p>This is a simple React component for displaying about information.</p>
        </div>
    );
 
}